1.
Yes, it is secure. We encrypeted the information with our public key first,
and the information must be decrypted by the server's private key.
Even if they get the public key, they still cannot decrypt without private key.

  (1)
  No, they cannot get the information. Because the public key is not related to the keyboard activity but is generated by algorithms.

  (2)
  Yes, becasue they can get both public and private key.

2.
--verifty can only check the correctness of its signature and it doesnt suggest anything about the owner or creater, so attacker can still add new signature with the personal information

One solution is sent the public key along with file. When they open it, they can 
check whether it is the correct public key instead of other public key made by others.